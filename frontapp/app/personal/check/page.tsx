"use client";

import { useState, useEffect } from "react";

export default function Check() {
  const [Check, setCheck] = useState([]);

  useEffect(() => {
    setChecked();
  }, []);

  const setChecked = () => {
  }

  return (
    <>
      <h4></h4>
      <div>{}</div>
    </>
  );
}